// Placeholder consumer file — actual code per test case is supplied inline by the rule tester
// using this file's path as the parser filename, which lets module resolution find the adjacent
// `node_modules/fake-pkg` and `src/shared/local-thing`.
export const FROM_MATCH_FIXTURE_MARKER = true;
