---
title: require-react-display-names
badges:
    - text: "error"
      variant: danger
description: Require `displayName` on exported `memo(...)` components and exported `createContext(...)` values.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Requires `displayName` on exported memoized components and exported contexts.

## Rule details :badge[Problem]{variant=danger}

The rule tracks calls to `memo(...)` and `createContext(...)` from the configured React environment.

It reports two cases:

- direct default exports such as `export default memo(Component)` or `export default createContext(...)`
- exported variables created from `memo(...)` or `createContext(...)` that never get a `.displayName` assignment

By default, `environment` is `"roblox-ts"`. Switch to `"standard"` to use regular React package sources.

<Aside type="tip" title="What counts as a display name assignment">
	The rule looks for `SomeValue.displayName = ...`. It does not require the string to match the variable name, but
	that is the intended pattern.
</Aside>

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={["export default memo(ButtonImpl);", "", "export const UserContext = createContext(undefined);"].join(
			"\n",
		)}
		lang="tsx"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={[
			"const Button = memo(ButtonImpl);",
			'Button.displayName = "Button";',
			"export default Button;",
			"",
			"const UserContext = createContext(undefined);",
			'UserContext.displayName = "UserContext";',
			"export { UserContext };",
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

## Options

| Option        | Type                        | Default       |
| :------------ | :-------------------------- | :------------ |
| `environment` | `"roblox-ts" \| "standard"` | `"roblox-ts"` |

## Related rules

<CardGrid>
	<LinkCard
		title="require-react-component-keys"
		description="Checks list rendering details in JSX"
		href="/eslint-cease-nonsense-rules/rules/require-react-component-keys/"
	/>
</CardGrid>
