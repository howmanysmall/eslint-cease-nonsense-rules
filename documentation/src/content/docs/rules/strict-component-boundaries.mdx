---
title: strict-component-boundaries
badges:
    - text: "error"
      variant: danger
description: Block relative imports that reach too far into another component folder.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Blocks relative imports that reach into another component folder.

## Rule details :badge[Problem]{variant=danger}

This rule only checks relative imports.

It resolves the import path, looks for PascalCase component directories in the target path, and reports deep imports
that reach into another component's internals instead of importing from that component's boundary.

By default, `maxDepth` is `1`, which means imports can reach the component root but not deeper nested files. Imports to
an `index` file are allowed, fixture paths under `fixtures` are allowed in the expected cases, and you can add regex
patterns to `allow` for local exceptions.

<Aside type="tip" title="What the message wants">
	The rule message points you back to the closest shared component boundary, usually the sibling component root or its
	`index` file.
</Aside>

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={[
			'import { helper } from "../UserCard/utils/helper";',
			'import { useProfile } from "../UserProfile/hooks/useProfile";',
		].join("\n")}
		lang="ts"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={['import { UserCard } from "../UserCard";', 'import { localHelper } from "./utils/localHelper";'].join(
			"\n",
		)}
		lang="ts"
	/>
</RuleExample>

## Options

| Option     | Type       | Default | Notes                                                                |
| :--------- | :--------- | :------ | :------------------------------------------------------------------- |
| `allow`    | `string[]` | `[]`    | Regex patterns matched case-insensitively against the import source. |
| `maxDepth` | `number`   | `1`     | Maximum allowed depth into another component path.                   |

## Related rules

<CardGrid>
	<LinkCard
		title="require-module-level-instantiation"
		description="Keeps shared runtime objects in one place"
		href="/eslint-cease-nonsense-rules/rules/require-module-level-instantiation/"
	/>
</CardGrid>
