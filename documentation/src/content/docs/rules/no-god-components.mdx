---
title: no-god-components
badges:
    - text: "error"
      variant: danger
description: Report React components that cross size and complexity limits.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Report React components that have grown past the limits this rule sets.

## Rule details :badge[Problem]{variant=danger}

This rule checks function components whose names start with an uppercase letter. It also checks components wrapped in
`memo` or `forwardRef`.

By default it reports these cases:

- more than `200` lines in a component
- more than `120` lines when `enforceTargetLines` is `true`
- more than `5` destructured props in the first parameter
- more than `3` levels of JSX or TSX nesting
- more than `5` state hook calls, counting `useState`, `useReducer`, and `useBinding`
- any runtime `null` literal inside the component body

Nested functions do not count toward JSX depth or hook usage. `null` literal type annotations are ignored.

<Aside type="caution" title="This rule reports several separate signals">
	A single component can produce more than one report. For example, one component may be too long, too deeply nested,
	and also contain `null` literals.
</Aside>

## Options

<Code
	code={[
		'import plugin from "eslint-plugin-cease-nonsense";',
		"",
		"export default [",
		"\t{",
		'\t\tplugins: { "cease-nonsense": plugin },',
		"\t\trules: {",
		'\t\t\t"cease-nonsense/no-god-components": [',
		'\t\t\t\t"error",',
		"\t\t\t\t{",
		"\t\t\t\t\tmaxLines: 180,",
		"\t\t\t\t\ttargetLines: 100,",
		"\t\t\t\t\tmaxDestructuredProps: 4,",
		"\t\t\t\t\tmaxTsxNesting: 2,",
		"\t\t\t\t\tmaxStateHooks: 4,",
		'\t\t\t\t\tstateHooks: ["useState", "useReducer", "useBinding"],',
		'\t\t\t\t\tignoreComponents: ["App"],',
		"\t\t\t\t},",
		"\t\t\t],",
		"\t\t},",
		"\t},",
		"];",
	].join("\n")}
	lang="typescript"
	title="eslint.config.ts"
/>

Options and defaults:

- `enforceTargetLines`, default `true`
- `targetLines`, default `120`
- `maxLines`, default `200`
- `maxDestructuredProps`, default `5`
- `maxTsxNesting`, default `3`
- `maxStateHooks`, default `5`
- `stateHooks`, default `['useState', 'useReducer', 'useBinding']`
- `ignoreComponents`, default `[]`

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={[
			"function Dashboard({ a, b, c, d, e, f }) {",
			"\tconst [user, setUser] = useState();",
			"\tconst [theme, setTheme] = useState();",
			"\tconst [notifications, setNotifications] = useState([]);",
			"\tconst [data, setData] = useState();",
			"\tconst [filter, setFilter] = useState('');",
			"\tconst [sort, setSort] = useReducer(reducer, initialState);",
			"",
			"\treturn (",
			"\t\t<div>",
			"\t\t\t<section>",
			"\t\t\t\t<div>",
			"\t\t\t\t\t<span>{null}</span>",
			"\t\t\t\t</div>",
			"\t\t\t</section>",
			"\t\t</div>",
			"\t);",
			"}",
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={[
			"function Dashboard() {",
			"\tconst { user, notifications } = useDashboardData();",
			"\tconst filters = useDashboardFilters();",
			"",
			"\treturn (",
			"\t\t<Layout>",
			"\t\t\t<Header user={user} />",
			"\t\t\t<MainContent data={notifications} filters={filters} />",
			"\t\t</Layout>",
			"\t);",
			"}",
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

## Related rules

<CardGrid>
	<LinkCard
		title="React: thinking in React"
		description="React's guide for breaking a large UI into smaller pieces."
		href="https://react.dev/learn/thinking-in-react"
	/>
</CardGrid>
