---
title: no-new-instance-in-use-memo
badges:
    - text: "error"
      variant: danger
    - text: "React"
      variant: note
description: Disallow configured constructor calls inside React useMemo callbacks.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Reports configured `new` expressions inside `useMemo` callbacks, including traced local helper calls.

## Rule details :badge[Problem]{variant=danger}

By default, the rule reports `new Instance(...)` inside `useMemo`. It also follows local helper functions called from
the memo callback, up to `maxHelperTraceDepth`, so a helper that creates an instance is still reported.

The rule only checks `useMemo` imported from the configured React environment. `environment: "roblox-ts"` watches
`@rbxts/react`, and `environment: "standard"` watches `react`.

<Aside type="tip" title="What maxHelperTraceDepth does">
	`0` only checks code written directly inside the `useMemo` callback. The default is `4`, which also traces a few
	helper calls.
</Aside>

## Examples

<RuleExample type="fail" title="Incorrect">
		### Constructor inside useMemo

        	<Code
        		code={[
        			'import { useMemo } from "@rbxts/react";',
        			"",
        			"const model = useMemo(() => {",
        			'\treturn new Instance("Model");',
        			"}, []);",
        		].join("\n")}
        		lang="tsx"
        	/>
    </RuleExample>

<RuleExample type="fail" title="Also incorrect">
	    	### Helper traced from useMemo

        	<Code
        		code={[
        			'import { useMemo } from "@rbxts/react";',
        			"",
        			"function createModel() {",
        			'\treturn new Instance("Model");',
        			"}",
        			"",
        			"const model = useMemo(() => createModel(), []);",
        		].join("\n")}
        		lang="tsx"
        	/>
    </RuleExample>

<RuleExample type="pass" title="Correct">
	    	### Reuse or create outside the memo callback

        	<Code
        		code={[
        			'import { useMemo } from "@rbxts/react";',
        			"",
        			'const model = new Instance("Model");',
        			"",
        			"const memoizedModel = useMemo(() => model, []);",
        		].join("\n")}
        		lang="tsx"
        	/>
    </RuleExample>

## Options

| Option                | Type                        | Default        | Notes                                                                           |
| :-------------------- | :-------------------------- | :------------- | :------------------------------------------------------------------------------ |
| `constructors`        | `string[]`                  | `["Instance"]` | Constructor names to report.                                                    |
| `environment`         | `"roblox-ts" \| "standard"` | `"roblox-ts"`  | Picks `@rbxts/react` or `react` imports.                                        |
| `maxHelperTraceDepth` | `integer`                   | `4`            | Helper-call depth to trace from the memo callback. `0` disables helper tracing. |

```js
import ceaseNonsense from "eslint-plugin-cease-nonsense";

export default [
	{
		plugins: {
			"cease-nonsense": ceaseNonsense,
		},
		rules: {
			"cease-nonsense/no-new-instance-in-use-memo": [
				"error",
				{
					constructors: ["Instance", "Vector3"],
					environment: "roblox-ts",
					maxHelperTraceDepth: 2,
				},
			],
		},
	},
];
```

## Related rules

<CardGrid>
	<LinkCard
		title="no-unused-use-memo"
		description="Disallows useMemo calls that are only used for side effects"
		href="/eslint-cease-nonsense-rules/rules/no-unused-use-memo/"
	/>
	<LinkCard
		title="no-useless-use-effect"
		description="Reports effects that are not syncing with anything external"
		href="/eslint-cease-nonsense-rules/rules/no-useless-use-effect/"
	/>
</CardGrid>
