---
title: prefer-udim2-shorthand
badges:
    - text: "Fixable"
      variant: success
    - text: "Suggestion"
      variant: note
description: Prefer `UDim2.fromScale()` or `UDim2.fromOffset()` when the full constructor only uses one side.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Replaces `new UDim2(...)` with `UDim2.fromScale(...)` or `UDim2.fromOffset(...)` when the other half is zero.

## Rule details :badge[Suggestion]{variant=note}

The rule checks `new UDim2(scaleX, offsetX, scaleY, offsetY)` calls with exactly four arguments.

- If `offsetX` and `offsetY` both evaluate to `0`, it reports `UDim2.fromScale(scaleX, scaleY)`.
- If `scaleX` and `scaleY` both evaluate to `0`, it reports `UDim2.fromOffset(offsetX, offsetY)`.

It can evaluate numeric literals, identifiers, unary `+` and `-`, and simple arithmetic with `+`, `-`, `*`, `/`, and
`%`. Mixed scale and offset constructors are left alone.

<Aside type="tip" title="Special case">
	`new UDim2(0, 0, 0, 0)` is allowed and is not rewritten.
</Aside>

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={["new UDim2(1, 0, 1, 0);", "new UDim2(0, 100, 0, 50);", "new UDim2(x + 1, 0, 1 - y, 0);"].join("\n")}
		lang="tsx"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={[
			"UDim2.fromScale(1, 1);",
			"UDim2.fromOffset(100, 50);",
			"UDim2.fromScale(x + 1, 1 - y);",
			"",
			"new UDim2(0.5, 10, 0.5, 10);",
			"new UDim2(0, 0, 0, 0);",
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

## What gets skipped

- constructors with fewer or more than four arguments
- constructors where the zero side cannot be evaluated
- unsupported expressions such as bitwise operators or string literals
- mixed scale and offset values

## Related rules

<CardGrid>
	<LinkCard
		title="prefer-sequence-overloads"
		description="Uses shorter Roblox constructor overloads for simple sequence values"
		href="/eslint-cease-nonsense-rules/rules/prefer-sequence-overloads/"
	/>
	<LinkCard
		title="prefer-pattern-replacements"
		description="Handles configurable constructor and method rewrites"
		href="/eslint-cease-nonsense-rules/rules/prefer-pattern-replacements/"
	/>
</CardGrid>
