---
title: no-async-constructor
badges:
    - text: "error"
      variant: danger
description: Disallows async work inside constructors.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Disallows async work inside constructors.

## Rule details :badge[Problem]{variant=danger}

Constructors return before async work finishes. This rule reports patterns that leave an instance half-initialized or
drop promise errors on the floor.

It reports:

- `await` inside a constructor
- promise chains like `.then()`, `.catch()`, and `.finally()`
- async IIFEs called from the constructor
- bare calls to async methods on `this`, such as `this.load()`
- promises from async methods stored in a local variable and never used

It does not report `this.loadPromise = this.load();`, because the promise stays on the instance and can be consumed by
callers.

<Aside type="danger" title="No options">
	This rule has no configuration. If async setup is required, move it to a factory or an explicit init step.
</Aside>

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={[
			"class Database {",
			"\tconstructor() {",
			"\t\tthis.connect().then(() => logReady());",
			"\t}",
			"",
			"\tasync connect() {}",
			"}",
		].join("\n")}
		lang="ts"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={[
			"class Database {",
			"\tprivate constructor() {}",
			"",
			"\tstatic async create() {",
			"\t\tconst database = new Database();",
			"\t\tawait database.connect();",
			"\t\treturn database;",
			"\t}",
			"",
			"\tasync connect() {}",
			"}",
		].join("\n")}
		lang="ts"
	/>
</RuleExample>

## Common refactors

- use `static async create()` when construction depends on async setup
- expose an `init()` method and make callers await it before use
- if you must start work in the constructor, store the promise on `this` so the lifecycle is visible

## Related rules

<CardGrid>
	<LinkCard
		title="require-module-level-instantiation"
		description="Keeps singleton-style setup at module scope"
		href="/eslint-cease-nonsense-rules/rules/require-module-level-instantiation/"
	/>
</CardGrid>
