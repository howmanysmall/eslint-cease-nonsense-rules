---
title: no-array-constructor-elements
badges:
    - text: "error"
      variant: danger
    - text: "Fixable"
      variant: success
description: Disallows most `new Array(...)` element forms and prefers literals or typed empty arrays.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Disallows `new Array(...)` forms that read like element lists and catches a few array initialization patterns that are
easy to rewrite.

## Rule details :badge[Problem]{variant=danger}

By default, this rule runs in `roblox-ts` mode and requires a type for `new Array()` with no arguments.

It reports these cases:

- `new Array("a", "b")` and similar enumeration forms, usually fixed to `[...]`
- `new Array("a")`, fixed to `["a"]`
- `new Array()` without type arguments or contextual `Array<T>` / `ReadonlyArray<T>` typing
- `const items = new Array<T>(); items.push(...)` when the pushes are immediate and consecutive

In `standard` mode, `new Array(length)` is also reported and gets an `Array.from({ length })` suggestion.

In `roblox-ts` mode, length-style constructors stay allowed. The rule also leaves alone roblox-ts overloads such as
`new Array(length, fill)` when the first argument could be numeric.

<Aside type="tip" title="What counts as contextual typing">
	`const items: Array<string> = new Array();` is valid. So is a matching `ReadonlyArray<T>` annotation or assertion.
</Aside>

## Configuration

<Code
	code={[
		'import ceaseNonsense from "eslint-plugin-cease-nonsense";',
		"",
		"export default [",
		"\t{",
		"\t\tplugins: {",
		'\t\t\t"cease-nonsense": ceaseNonsense,',
		"\t\t},",
		"\t\trules: {",
		'\t\t\t"cease-nonsense/no-array-constructor-elements": ["error", {',
		'\t\t\t\t"environment": "standard",',
		'\t\t\t\t"requireExplicitGenericOnNewArray": true',
		"\t\t\t}],",
		"\t\t},",
		"\t},",
		"];",
	].join("\n")}
	lang="ts"
	title="eslint.config.ts"
/>

## Options

| Option                             | Default       | What it does                                                                                           |
| :--------------------------------- | :------------ | :----------------------------------------------------------------------------------------------------- |
| `environment`                      | `"roblox-ts"` | `"roblox-ts"` allows length-style `new Array(...)` overloads, `"standard"` reports `new Array(length)` |
| `requireExplicitGenericOnNewArray` | `true`        | Requires `new Array<T>()` or matching contextual array typing for zero-argument calls                  |

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={[
			'const letters = new Array("a", "b");',
			'const single = new Array("a");',
			"const missingType = new Array();",
			"",
			"const items = new Array<string>();",
			'items.push("a");',
			'items.push("b");',
		].join("\n")}
		lang="ts"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={[
			'const letters = ["a", "b"];',
			'const single = ["a"];',
			"const typed = new Array<string>();",
			"const contextual: Array<string> = new Array();",
			"const sized = new Array(10); // valid in roblox-ts mode",
		].join("\n")}
		lang="ts"
	/>
</RuleExample>

## Push collapse behavior

The `new Array<T>()` plus `.push(...)` rewrite only happens when all of these are true:

- the declaration is a single `const` or `let`
- the `push` calls are immediate and consecutive in the same block
- there is no later `push` call on the same variable in that block or program section
- every pushed value is safe to inline, otherwise the rule gives a suggestion instead of an automatic fix

## Related rules

<CardGrid>
	<LinkCard
		title="no-empty-array-literal"
		description="Checks empty array literal usage"
		href="/eslint-cease-nonsense-rules/rules/no-empty-array-literal/"
	/>
	<LinkCard
		title="no-array-size-assignment"
		description="Replaces append-by-size indexing with `push`"
		href="/eslint-cease-nonsense-rules/rules/no-array-size-assignment/"
	/>
</CardGrid>
