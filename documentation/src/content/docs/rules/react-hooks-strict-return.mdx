---
title: react-hooks-strict-return
badges:
    - text: "Suggestion"
      variant: note
description: Keep custom hook returns to an object or a tuple with at most two items.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Limits custom hooks to returning an object or a tuple with at most two items.

## Rule details :badge[Suggestion]{variant=note}

The rule checks functions whose names look like hooks, such as `useThing`, and reports returns that resolve to more than
two tuple items.

Returning an object is always allowed. Returning one or two array items is also allowed. Once a hook returns three or
more positional values, the rule reports it and asks you to switch to named properties instead.

<Aside type="tip" title="What the rule counts">
	The rule follows direct array returns, returned array variables, and simple spread cases, so `return values` and
	`return [a, ...rest]` are checked too.
</Aside>

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code code={["function useData() {", "\treturn [state, setState, loading, error];", "}"].join("\n")} lang="tsx" />
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={[
			"function useToggle() {",
			"\treturn [isOpen, setIsOpen];",
			"}",
			"",
			"function useData() {",
			"\treturn { state, setState, loading, error };",
			"}",
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

## Related rules

<CardGrid>
	<LinkCard
		title="require-named-effect-functions"
		description="Keeps effect callbacks readable in stack traces"
		href="/eslint-cease-nonsense-rules/rules/require-named-effect-functions/"
	/>
	<LinkCard
		title="use-hook-at-top-level"
		description="Keeps hook calls in a predictable place"
		href="/eslint-cease-nonsense-rules/rules/use-hook-at-top-level/"
	/>
</CardGrid>
