---
title: prefer-padding-components
badges:
    - text: "Fixable"
      variant: success
description: Prefer local EqualPadding or DirectionalPadding components over matching uipadding elements.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Prefer local padding components when a `<uipadding />` element already matches them.

## Rule details :badge[Suggestion]{variant=note}

This rule checks empty `<uipadding />` elements with exactly these four attributes: `PaddingTop`, `PaddingBottom`,
`PaddingLeft`, and `PaddingRight`.

It reports two cases:

| Shape                                                                          | Report               |
| :----------------------------------------------------------------------------- | :------------------- |
| All four values are the same                                                   | `EqualPadding`       |
| `PaddingTop` matches `PaddingBottom`, and `PaddingLeft` matches `PaddingRight` | `DirectionalPadding` |

The rule only reports when the project already has a matching local component.

<Aside type="tip" title="When it skips">
	The rule does nothing when the element has spread attributes, duplicate padding attributes, extra attributes, or
	real children.
</Aside>

## Examples

<RuleExample type="fail" title="Incorrect">
		### Repeated padding attributes

        	<Code
        		code={[
        			"<uipadding",
        			"\tPaddingBottom={padding}",
        			"\tPaddingLeft={padding}",
        			"\tPaddingRight={padding}",
        			"\tPaddingTop={padding}",
        			"/>;",
        		].join("\n")}
        		lang="tsx"
        	/>
    </RuleExample>

<RuleExample type="pass" title="Correct">
		### Matching local components

        	<Code
        		code={[
        			"<EqualPadding padding={padding} />;",
        			"",
        			"<DirectionalPadding horizontal={horizontal} vertical={vertical} />;",
        		].join("\n")}
        		lang="tsx"
        	/>
    </RuleExample>

## Auto-fix behavior

Auto-fix only runs in `.jsx` and `.tsx` files, and only when the current file already imports exactly one matching local
component.

For `DirectionalPadding`, the generated props follow the local component contract used by the rule:

- `horizontal` comes from `PaddingTop` and `PaddingBottom`
- `vertical` comes from `PaddingLeft` and `PaddingRight`

## Related rules

<CardGrid>
	<LinkCard
		title="prefer-udim2-shorthand"
		description="Replaces repeated Roblox constructor patterns with shorter forms"
		href="/eslint-cease-nonsense-rules/rules/prefer-udim2-shorthand/"
	/>
	<LinkCard
		title="prefer-context-stack"
		description="Pushes repeated JSX structure into a shared component"
		href="/eslint-cease-nonsense-rules/rules/prefer-context-stack/"
	/>
</CardGrid>
