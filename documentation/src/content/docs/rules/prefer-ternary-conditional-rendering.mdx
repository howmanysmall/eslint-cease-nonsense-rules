---
title: prefer-ternary-conditional-rendering
badges:
    - text: "Fixable"
      variant: success
    - text: "Suggestion"
      variant: note
description: Prefer one ternary when two sibling JSX `&&` branches are logical opposites.
---

import { Aside, Code, CardGrid, LinkCard } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Reports sibling JSX `&&` branches when the conditions are logical opposites and can be written as one ternary.

## Rule details :badge[Suggestion]{variant=note}

The rule looks at adjacent JSX expression containers, ignoring whitespace-only JSX text between them.

It reports pairs like these:

- `flag && <A />` and `!flag && <B />`
- `mode === "x" && <A />` and `mode !== "x" && <B />`
- the same strict comparison with the operands swapped

The fixer is narrower than the report. It only rewrites pairs when the condition is safe to reuse directly in a ternary:

- negation pairs built from an identifier, `this`, or a literal
- strict comparison pairs where both sides are identifiers, `this`, or literals

<Aside type="note" title="Reported without a fix">
	Pairs like `isReady() && <A />` and `!isReady() && <B />` are still reported, but there is no auto-fix.
</Aside>

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={[
			"function Component({ flag, mode }) {",
			"\treturn (",
			"\t\t<>",
			"\t\t\t{flag && <A />}",
			"\t\t\t{!flag && <B />}",
			'\t\t\t{mode === "x" && <Primary />}',
			'\t\t\t{mode !== "x" && <Fallback />}',
			"\t\t</>",
			"\t);",
			"}",
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={[
			"function Component({ flag, mode }) {",
			"\treturn (",
			"\t\t<>",
			"\t\t\t{flag ? <A /> : <B />}",
			'\t\t\t{mode === "x" ? <Primary /> : <Fallback />}',
			"\t\t</>",
			"\t);",
			"}",
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

## What gets skipped

- non-adjacent branches
- pairs separated by real JSX content, not just whitespace
- `&&` expressions whose right side is not a JSX element or fragment
- unrelated conditions like `first && <A />` and `second && <B />`

## Related rules

<CardGrid>
	<LinkCard
		title="use-hook-at-top-level"
		description="Keeps hook calls in predictable places"
		href="/eslint-cease-nonsense-rules/rules/use-hook-at-top-level/"
	/>
	<LinkCard
		title="prefer-read-only-props"
		description="Prevents prop mutation in React components"
		href="/eslint-cease-nonsense-rules/rules/prefer-read-only-props/"
	/>
</CardGrid>
