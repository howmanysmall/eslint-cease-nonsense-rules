---
title: require-react-component-keys
badges:
    - text: "error"
      variant: danger
description:
    Require keys for JSX returned from list-like callbacks, and flag unnecessary keys on root returns by default.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Requires keys for JSX used as list output.

## Rule details :badge[Problem]{variant=danger}

The rule looks for JSX returned from iteration callbacks such as `.map(...)` and from memoization callbacks such as
`useMemo(...)`. In those contexts, JSX elements need a `key`, and JSX fragments need to be replaced with a keyed
element.

It skips ordinary top-level component returns, JSX passed through most props, ternary children, logical child fragments,
and a small ignore list of call expressions. By default it also reports a `key` on a plain root component return,
because that key is usually ignored by the parent.

<Aside type="tip" title="Fragments are stricter here">
	A bare fragment like `<>...</>` can never carry a key, so list items that use fragments need `<React.Fragment key=...>`
	or another keyed wrapper element.
</Aside>

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={["function UserList({ users }) {", "\treturn users.map((user) => <UserCard user={user} />);", "}"].join(
			"\n",
		)}
		lang="tsx"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={[
			"function UserList({ users }) {",
			"\treturn users.map((user) => <UserCard key={user.id} user={user} />);",
			"}",
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

## Options

| Option                  | Type       | Default                                                                                                  |
| :---------------------- | :--------- | :------------------------------------------------------------------------------------------------------- |
| `allowRootKeys`         | `boolean`  | `false`                                                                                                  |
| `ignoreCallExpressions` | `string[]` | `["ReactTree.mount", "CreateReactStory"]`                                                                |
| `iterationMethods`      | `string[]` | `["map", "filter", "forEach", "flatMap", "reduce", "reduceRight", "some", "every", "find", "findIndex"]` |
| `memoizationHooks`      | `string[]` | `["useCallback", "useMemo"]`                                                                             |

## Related rules

<CardGrid>
	<LinkCard
		title="require-react-display-names"
		description="Keeps React DevTools output readable"
		href="/eslint-cease-nonsense-rules/rules/require-react-display-names/"
	/>
</CardGrid>
