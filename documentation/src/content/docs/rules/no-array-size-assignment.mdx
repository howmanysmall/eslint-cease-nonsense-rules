---
title: no-array-size-assignment
badges:
    - text: "error"
      variant: danger
    - text: "Fixable"
      variant: success
description: Reports `array[array.size()] = value` and can rewrite it to `array.push(value)`.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Reports append-by-size assignments and points you to `push`.

## Rule details :badge[Problem]{variant=danger}

The rule matches this exact append pattern:

```ts
array[array.size()] = value;
```

It also works with member chains such as `state.items[state.items.size()] = item;`.

It does not match lookalikes like `array[array.size() - 1] = value`, `array[other.size()] = value`, or
`array[array.size()] += value`.

<Aside type="tip" title="Default behavior">
	Autofix is off by default. With the default config, the rule reports the pattern but does not rewrite it.
</Aside>

## Configuration

<Code
	code={[
		'import ceaseNonsense from "eslint-plugin-cease-nonsense";',
		"",
		"export default [",
		"\t{",
		"\t\tplugins: {",
		'\t\t\t"cease-nonsense": ceaseNonsense,',
		"\t\t},",
		"\t\trules: {",
		'\t\t\t"cease-nonsense/no-array-size-assignment": ["error", {',
		'\t\t\t\t"allowAutofix": true',
		"\t\t\t}],",
		"\t\t},",
		"\t},",
		"];",
	].join("\n")}
	lang="ts"
	title="eslint.config.ts"
/>

## Option

| Option         | Default | What it does                                                                     |
| :------------- | :------ | :------------------------------------------------------------------------------- |
| `allowAutofix` | `false` | Rewrites the assignment to `.push(...)` when the target is safe to evaluate once |

The automatic fix only runs when the assignment is a standalone expression statement and the array target is a simple,
non-optional reference chain such as `array`, `this.items`, `store["items"]`, or `registry[keyRef.value]`.

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={["inventory[inventory.size()] = item;", "state.items[state.items.size()] = nextItem;"].join("\n")}
		lang="ts"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code code={["inventory.push(item);", "state.items.push(nextItem);"].join("\n")} lang="ts" />
</RuleExample>

## Related rules

<CardGrid>
	<LinkCard
		title="no-array-constructor-elements"
		description="Cleans up other array initialization patterns"
		href="/eslint-cease-nonsense-rules/rules/no-array-constructor-elements/"
	/>
</CardGrid>
