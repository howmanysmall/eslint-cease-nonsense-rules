---
title: require-serialized-numeric-data-type
badges:
    - text: "error"
      variant: danger
description: Require `DataType.*` instead of raw `number` in configured serialization type positions.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Requires `DataType.*` instead of raw `number` in serialization type positions.

## Rule details :badge[Problem]{variant=danger}

In the default `type-arguments` mode, the rule only checks type arguments passed to selected functions. By default, that
means `registerComponent<...>()`.

It reports raw `number` and nested uses of `number`, including unions, intersections, arrays, tuples, type literals,
indexed access types, and generic type arguments. `DataType.u8`, `DataType.i16`, `DataType.f32`, and the other supported
`DataType.*` variants are allowed.

In `all` mode, the rule also checks function parameters, return types, properties, type aliases, and variable type
annotations across the file. When `strict: true`, it uses the TypeScript type checker so aliased `number` types can be
reported too.

<Aside type="tip" title="Supported DataType members">
	The rule recognizes `u8`, `u16`, `u32`, `i8`, `i16`, `i32`, `f32`, and `f64`.
</Aside>

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={[
			"const Health = registerComponent<number>({ replicated: true });",
			"",
			"type Payload = { value: number };",
		].join("\n")}
		lang="ts"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={[
			"const Health = registerComponent<DataType.u16>({ replicated: true });",
			"",
			"type Payload = { value: DataType.u16 };",
		].join("\n")}
		lang="ts"
	/>
</RuleExample>

## Options

| Option          | Type                        | Default                 |
| :-------------- | :-------------------------- | :---------------------- |
| `functionNames` | `string[]`                  | `["registerComponent"]` |
| `mode`          | `"type-arguments" \| "all"` | `"type-arguments"`      |
| `strict`        | `boolean`                   | `false`                 |

Example config:

```js
import ceaseNonsense from "eslint-plugin-cease-nonsense";

export default [
	{
		plugins: {
			"cease-nonsense": ceaseNonsense,
		},
		rules: {
			"cease-nonsense/require-serialized-numeric-data-type": [
				"error",
				{
					mode: "type-arguments",
					functionNames: ["registerComponent", "createEvent"],
				},
			],
		},
	},
];
```

## Related rules

<CardGrid>
	<LinkCard
		title="require-module-level-instantiation"
		description="Another rule aimed at long-lived runtime behavior"
		href="/eslint-cease-nonsense-rules/rules/require-module-level-instantiation/"
	/>
</CardGrid>
