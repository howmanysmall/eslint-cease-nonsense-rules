---
title: no-table-create-map
badges:
    - text: "error"
      variant: danger
    - text: "Suggestion"
      variant: note
description: Disallow calling map() directly on table.create(...) and one- or two-argument Array constructors.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Reports `.map(...)` called directly on `table.create(...)` or `new Array(...)`.

## Rule details :badge[Suggestion]{variant=note}

The rule only looks for the direct pattern. These are reported:

- `table.create(...).map(...)`
- `new Array(...).map(...)`

It does not report cases where you store the result first, add another method call before `map`, or shadow `table` or
`Array` with your own local definitions.

For `new Array(...)`, the rule only matches constructors with one or two arguments.

<Aside type="tip" title="What to write instead">
	Allocate the array first, then fill it in a loop or map from an existing array value you already named.
</Aside>

## Examples

<RuleExample type="fail" title="Incorrect">
		### Direct construct-then-map chain

        	<Code
        		code={[
        			"const mapped = table.create(total, fallback).map((value) => value);",
        			"const rewards = new Array<Reward>(size).map(() => makeReward());",
        		].join("\n")}
        		lang="ts"
        	/>
    </RuleExample>

<RuleExample type="pass" title="Correct">
	    	### Allocate first, then fill

        	<Code
        		code={[
        			"const rewards = new Array<Reward>(size);",
        			"for (const index of $range(1, size)) {",
        			"\trewards[index - 1] = makeReward();",
        			"}",
        			"",
        			"const baseRewards = table.create(size, fallback);",
        			"const mappedRewards = baseRewards.map((value) => value);",
        		].join("\n")}
        		lang="ts"
        	/>
    </RuleExample>

## Options

This rule has no options.

## Related rules

<CardGrid>
	<LinkCard
		title="no-array-size-assignment"
		description="Checks another array-construction pattern"
		href="/eslint-cease-nonsense-rules/rules/no-array-size-assignment/"
	/>
	<LinkCard
		title="prefer-sequence-overloads"
		description="Covers another Roblox-focused collection pattern"
		href="/eslint-cease-nonsense-rules/rules/prefer-sequence-overloads/"
	/>
</CardGrid>
