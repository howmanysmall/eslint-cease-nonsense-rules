---
title: prefer-singular-enums
---

import { Code } from "@astrojs/starlight/components";

Enum names should be singular, not plural.

## Rule Details

Enum type names represent a single value from a set, not multiple values. Using singular names makes code more readable and aligns with TypeScript conventions.

## Examples

### ❌ Incorrect

<Code
	code={[
		"enum Colors {",
		"\tRed,",
		"\tBlue,",
		"\tGreen,",
		"}",
		"",
		"enum Commands {",
		"\tUp,",
		"\tDown,",
		"\tLeft,",
		"\tRight,",
		"}",
		"",
		"// Irregular plurals also caught",
		"enum Feet {",
		"\tLeft,",
		"\tRight,",
		"}",
		"",
		"enum People {",
		"\tAdmin,",
		"\tUser,",
		"\tGuest,",
		"}",
		"",
		"enum Children {",
		"\tInfant,",
		"\tToddler,",
		"\tTeen,",
		"}",
	].join("\n")}
	lang="typescript"
	key="enum-colors-3b35zf"
/>

### ✅ Correct

<Code
	code={[
		"enum Color {",
		"\tRed,",
		"\tBlue,",
		"\tGreen,",
		"}",
		"",
		"enum Command {",
		"\tUp,",
		"\tDown,",
		"\tLeft,",
		"\tRight,",
		"}",
		"",
		"enum Foot {",
		"\tLeft,",
		"\tRight,",
		"}",
		"",
		"enum Person {",
		"\tAdmin,",
		"\tUser,",
		"\tGuest,",
		"}",
		"",
		"enum Child {",
		"\tInfant,",
		"\tToddler,",
		"\tTeen,",
		"}",
	].join("\n")}
	lang="typescript"
	key="enum-color-fbh2yy"
/>

## Why Singular?

### Represents Single Values

When you use an enum, you're selecting **one** value:

<Code
	code={[
		"// Confusing: sounds like multiple colors",
		"const background: Colors = Colors.Red;",
		"",
		"// Clear: one color",
		"const background: Color = Color.Red;",
	].join("\n")}
	lang="typescript"
	key="confusing-sounds-lik-mfwy68"
/>

### Consistent with TypeScript

TypeScript conventions use singular names for types representing single values:

<Code
	code={[
		"type User = {",
		"\t/* ... */",
		"};",
		"interface Product {",
		"\t/* ... */",
		"}",
		"enum Status {",
		"\t/* ... */",
		"}",
		"",
		"// Not:",
		"type Users = {",
		"\t/* ... */",
		"};",
		"interface Products {",
		"\t/* ... */",
		"}",
		"enum Statuses {",
		"\t/* ... */",
		"}",
	].join("\n")}
	lang="typescript"
	key="type-user-2m87vt"
/>

### Better in Context

<Code
	code={[
		"// Awkward",
		"function setTheme(colors: Colors) {",
		"\tapplyColor(colors);",
		"}",
		"",
		"// Natural",
		"function setTheme(color: Color) {",
		"\tapplyColor(color);",
		"}",
	].join("\n")}
	lang="typescript"
	key="awkward-3bdz26"
/>

## Plurals

The rule detects both regular and irregular plural forms:

### Regular Plurals (add 's' or 'es')

- `Commands` → `Command`
- `Types` → `Type`
- `Statuses` → `Status`

### Irregular Plurals

- `Children` → `Child`
- `People` → `Person`
- `Feet` → `Foot`
- `Teeth` → `Tooth`
- `Mice` → `Mouse`
- `Geese` → `Goose`

## When Not To Use It

In rare cases where an enum truly represents a collection concept (like bit flags), plural names might be appropriate. However, these cases are uncommon, and you might want to use a different pattern:

<Code
	code={[
		"// Instead of plural enum",
		"enum Permissions {",
		"\tRead = 1,",
		"\tWrite = 2,",
		"\tExecute = 4,",
		"}",
		"",
		"// Consider bit flag pattern",
		"enum Permission {",
		"\tRead = 1 << 0,",
		"\tWrite = 1 << 1,",
		"\tExecute = 1 << 2,",
		"}",
		"",
		"type PermissionSet = number; // Represents multiple permissions",
	].join("\n")}
	lang="typescript"
	key="instead-of-plural-en-1a59ry"
/>

## Related Rules

- [prefer-pascal-case-enums](/rules/prefer-pascal-case-enums/) - Enforces PascalCase for enum names and members

## Further Reading

- [TypeScript Enum Best Practices](https://www.typescriptlang.org/docs/handbook/enums.html)
- [Naming Conventions](https://google.github.io/styleguide/tsguide.html#naming-style)
