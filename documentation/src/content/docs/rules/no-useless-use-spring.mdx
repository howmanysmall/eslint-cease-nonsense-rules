---
title: no-useless-use-spring
badges:
    - text: "error"
      variant: danger
    - text: "Suggestion"
      variant: note
description: Reports spring hook calls whose config is static and whose dependencies never update.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Reports spring hook calls whose config is static and whose dependencies never update.

## Rule details :badge[Suggestion]{variant=note}

By default, this rule checks `useSpring`. It reports calls where the config object is fully static and the dependency
array is missing, omitted, or static. Empty dependency arrays are also reported by default.

Calls with both `from` and `to` in the config are skipped, because they can still represent a one-time mount animation.

<Aside type="danger" title="Static springs still cost work">
	If a spring can never change, the hook is extra machinery around a constant value.
</Aside>

## Options

| Option                      | Type       | Default                                                                      | What it does                           |
| --------------------------- | ---------- | ---------------------------------------------------------------------------- | -------------------------------------- |
| `springHooks`               | `string[]` | `["useSpring"]`                                                              | Hook names treated as spring hooks     |
| `staticGlobalFactories`     | `string[]` | Roblox value constructors such as `Vector3`, `UDim2`, `Color3`, and `CFrame` | Names treated as static constructors   |
| `treatEmptyDepsAsViolation` | `boolean`  | `true`                                                                       | Reports `[]` when the config is static |

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={[
			"const spring = useSpring({ opacity: 1 });",
			"",
			"const DEFAULT_SCALE = 1.5;",
			"const scale = useSpring({ scale: DEFAULT_SCALE }, [DEFAULT_SCALE]);",
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={[
			"const spring = useSpring({ opacity: isOpen ? 1 : 0 }, [isOpen]);",
			"",
			"const intro = useSpring({",
			"\tfrom: { scale: 0 },",
			"\tto: { scale: 1 },",
			"}, []);",
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

## Related rules

<CardGrid>
	<LinkCard
		title="no-useless-use-memo"
		description="Moves static memo values to module scope"
		href="/eslint-cease-nonsense-rules/rules/no-useless-use-memo/"
	/>
	<LinkCard
		title="use-exhaustive-dependencies"
		description="Checks hook dependency lists"
		href="/eslint-cease-nonsense-rules/rules/use-exhaustive-dependencies/"
	/>
	<LinkCard
		title="no-identity-map"
		description="Removes data transforms that do nothing"
		href="/eslint-cease-nonsense-rules/rules/no-identity-map/"
	/>
</CardGrid>
