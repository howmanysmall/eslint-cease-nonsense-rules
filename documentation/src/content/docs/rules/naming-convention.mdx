---
title: naming-convention
---

import { Code } from "@astrojs/starlight/components";

Enforce naming conventions for TypeScript constructs.

## Rule Details

This rule enforces naming conventions for interfaces, types, and other TypeScript constructs. It's optimized for common use cases like interface prefix checking without requiring type checking. Declaration files (`.d.ts`) are ignored by default.

## Options

<Code
	code={[
		"{",
		'\t"cease-nonsense/naming-convention": [',
		'\t\t"error",',
		"\t\t{",
		'\t\t\t"custom": {',
		'\t\t\t\t"match": false,',
		'\t\t\t\t"regex": "^I[A-Z]",',
		"\t\t\t},",
		'\t\t\t"format": ["PascalCase"],',
		'\t\t\t"selector": "interface",',
		"\t\t},",
		"\t],",
		"}",
	].join("\n")}
	lang="typescript"
	key="block-0-49ttwx"
/>

### Configuration Parameters

- **custom**: Custom regex matching options.
    - **match** (default: `true`): Whether the regex should match (`true`) or not match (`false`).
    - **regex**: Regular expression pattern to test against names.
- **format**: Array of format names (e.g., `["PascalCase"]`).
- **selector**: Type of construct to check (e.g., `"interface"`).

## Examples

### ❌ Incorrect

<Code
	code={['// With custom: { match: false, regex: "^I[A-Z]" }', "interface IUser {", "\tname: string;", "}"].join(
		"\n",
	)}
	lang="typescript"
	key="with-custom-match-fa-2g54hf"
/>

### ✅ Correct

<Code code={["interface User {", "\tname: string;", "}"].join("\n")} lang="typescript" key="interface-user-wv5yyt" />

## When Not To Use It

If your codebase intentionally uses interface prefixes (e.g., `IUser`) for legacy reasons or team preference, disable this rule.

## Performance

This rule is optimized with:

- No type checking required (fast AST-only analysis)
- Pre-compiled regex patterns
- Focused on common use cases

## Related Rules

- `@typescript-eslint/naming-convention` - Full-featured naming convention rule (slower, more comprehensive)
