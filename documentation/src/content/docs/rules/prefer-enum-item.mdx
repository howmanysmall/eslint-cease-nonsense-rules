---
title: prefer-enum-item
badges:
    - text: "Fixable"
      variant: success
    - text: "Suggestion"
      variant: note
description: Prefer Roblox `Enum` items over string and number literals when the expected type is an `EnumItem`.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Replaces string and number literals with Roblox `Enum` items when the expected type already points to one.

## Rule details :badge[Suggestion]{variant=note}

This rule uses type information. It reports a literal only when TypeScript can tell that the surrounding position
expects one or more Roblox `Enum` items and the literal matches one of them.

That means the rule can catch cases like:

- function and constructor arguments
- object property values
- JSX attribute values
- variable initializers with an explicit type annotation

<Aside type="tip" title="Numeric fixes can be configured">
	By default, a numeric literal fixes to `Enum.Type.Member`. Turn on `fixNumericToValue` if you want numeric matches
	to fix to `Enum.Type.Member.Value` instead.
</Aside>

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={[
			'<uiflexitem FlexMode="Fill" />',
			"",
			"const props: { ScaleType: Enum.ScaleType } = {",
			"\tScaleType: 1,",
			"};",
			"",
			'setFlexMode("Fill");',
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={[
			"<uiflexitem FlexMode={Enum.UIFlexMode.Fill} />",
			"",
			"const props: { ScaleType: Enum.ScaleType } = {",
			"\tScaleType: Enum.ScaleType.Slice,",
			"};",
			"",
			"setFlexMode(Enum.UIFlexMode.Fill);",
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

## Options

| Option              | Default | What it does                                                                      |
| ------------------- | ------- | --------------------------------------------------------------------------------- |
| `fixNumericToValue` | `false` | Fixes numeric literals to `Enum.Type.Member.Value` instead of `Enum.Type.Member`. |
| `performanceMode`   | `true`  | Uses cached enum lookups to skip work. It does not change what gets reported.     |

```js
// eslint.config.js
import ceaseNonsense from "eslint-plugin-cease-nonsense";

export default [
	{
		plugins: {
			"cease-nonsense": ceaseNonsense,
		},
		rules: {
			"cease-nonsense/prefer-enum-item": ["error", { fixNumericToValue: true }],
		},
	},
];
```

## Related rules

<CardGrid>
	<LinkCard
		title="prefer-enum-member"
		description="Does the same kind of check for TypeScript enums"
		href="/eslint-cease-nonsense-rules/rules/prefer-enum-member/"
	/>
	<LinkCard
		title="prefer-pascal-case-enums"
		description="Keeps enum names in a consistent style"
		href="/eslint-cease-nonsense-rules/rules/prefer-pascal-case-enums/"
	/>
</CardGrid>
