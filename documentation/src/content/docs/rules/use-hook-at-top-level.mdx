---
title: use-hook-at-top-level
badges:
    - text: "error"
      variant: danger
description: Require hooks to run at the top level of components and custom hooks.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Requires hooks to run at the top level of components and custom hooks.

## Rule details :badge[Problem]{variant=danger}

The rule looks for hook-like calls, names that start with `use` followed by a capital letter, inside component or custom
hook bodies.

It reports hooks called inside conditionals, loops, nested functions, try blocks, after an early return, and recursive
hook calls. Hooks inside `finally` blocks are allowed because that block always runs.

You can narrow or widen what gets checked with `onlyHooks`, `ignoreHooks`, and `importSources`. If `onlyHooks` is
present, it takes priority over the other two options.

<Aside type="tip" title="Import source filtering">
	`importSources` maps import sources or member namespaces to booleans. That lets you ignore one hook library without
	skipping every `use*` call in the file.
</Aside>

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={[
			"function UserProfile({ userId }) {",
			"\tif (userId) {",
			"\t\tuseEffect(() => {}, [userId]);",
			"\t}",
			"}",
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={[
			"function UserProfile({ userId }) {",
			"\tuseEffect(() => {",
			"\t\tif (userId) loadUser(userId);",
			"\t}, [userId]);",
			"}",
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

## Options

| Option          | Type                      | Default | Notes                                                        |
| :-------------- | :------------------------ | :------ | :----------------------------------------------------------- |
| `ignoreHooks`   | `string[]`                | `[]`    | Exact hook names to skip.                                    |
| `importSources` | `Record<string, boolean>` | `{}`    | `true` checks a source, `false` skips it.                    |
| `onlyHooks`     | `string[]`                | `[]`    | Whitelist mode. When present, the other filters are ignored. |

## Related rules

<CardGrid>
	<LinkCard
		title="use-exhaustive-dependencies"
		description="Checks the dependency array once the hook is in the right place"
		href="/eslint-cease-nonsense-rules/rules/use-exhaustive-dependencies/"
	/>
	<LinkCard
		title="require-named-effect-functions"
		description="Checks the callback shape passed to effect hooks"
		href="/eslint-cease-nonsense-rules/rules/require-named-effect-functions/"
	/>
</CardGrid>
