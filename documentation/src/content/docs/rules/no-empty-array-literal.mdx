---
title: no-empty-array-literal
badges:
    - text: "error"
      variant: danger
    - text: "Fixable"
      variant: success
description: Disallow empty array literals in places where the rule wants constructor form instead.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Disallow bare `[]` in places where the rule wants `new Array()` or `new Array<T>()` instead.

## Rule details :badge[Problem]{variant=danger}

This rule reports empty array literals with no elements.

It does not ban every `[]`. By default, several contexts are allowed, including call arguments, return statements,
object property values, JSX attributes, conditional branches, logical fallbacks, `for...of` right-hand sides, typed
assignment defaults, type assertions, and typed variable declarations that already spell out an array type.

When the rule can read an explicit element type, it auto-fixes to `new Array<T>()` by default. When it cannot, it still
reports the literal and offers `new Array()` as a suggestion.

<Aside type="tip" title="One option is kept for compatibility">
	`ignoreInferredNonEmptyLiterals` is accepted by the rule, but it does not change behavior. Non-empty array literals
	stay allowed either way.
</Aside>

## Options

### Main options

- `inferTypeForEmptyArrayFix`, default `false`, lets the rule ask TypeScript for a contextual element type before fixing
- `requireExplicitGenericOnNewArray`, default `true`, keeps `new Array<T>()` when the element type is known
- `allowedEmptyArrayContexts`, all keys default to `true`, controls which safe contexts can still use `[]`
- `ignoreInferredNonEmptyLiterals`, default `true`, accepted but has no effect

<Code
	code={[
		'import plugin from "eslint-plugin-cease-nonsense";',
		"",
		"export default [",
		"\t{",
		'\t\tplugins: { "cease-nonsense": plugin },',
		"\t\trules: {",
		'\t\t\t"cease-nonsense/no-empty-array-literal": [',
		'\t\t\t\t"error",',
		"\t\t\t\t{",
		"\t\t\t\t\tallowedEmptyArrayContexts: {",
		"\t\t\t\t\t\tcallArguments: false,",
		"\t\t\t\t\t\treturnStatements: false,",
		"\t\t\t\t\t},",
		"\t\t\t\t\tinferTypeForEmptyArrayFix: true,",
		"\t\t\t\t\trequireExplicitGenericOnNewArray: true,",
		"\t\t\t\t},",
		"\t\t\t],",
		"\t\t},",
		"\t},",
		"];",
	].join("\n")}
	lang="typescript"
	title="eslint.config.ts"
/>

### Allowed contexts

`allowedEmptyArrayContexts` supports these keys:

- `arrowFunctionBody`
- `assignmentExpressions`
- `assignmentPatterns`
- `callArguments`
- `conditionalExpressions`
- `forOfStatements`
- `jsxAttributes`
- `logicalExpressions`
- `propertyValues`
- `returnStatements`
- `typeAssertions`

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={["const values = [];", "", "class Store {", "\titems: Array<string> = [];", "\tqueue = [];", "}"].join(
			"\n",
		)}
		lang="tsx"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={[
			"const values: Array<string> = [];",
			"",
			"function readAll(input: Array<number> = []) {",
			"\treturn input;",
			"}",
			"",
			"const fallback = data || [];",
			"const seeded = [1, 2, 3];",
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

## Related rules

<CardGrid>
	<LinkCard
		title="array-type-generic"
		description="Enforce `Array<T>` and `ReadonlyArray<T>` type syntax."
		href="/eslint-cease-nonsense-rules/rules/array-type-generic/"
	/>
	<LinkCard
		title="no-array-constructor-elements"
		description="Check how `new Array(...)` is used once you adopt constructor form."
		href="/eslint-cease-nonsense-rules/rules/no-array-constructor-elements/"
	/>
</CardGrid>
