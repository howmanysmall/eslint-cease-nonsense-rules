---
title: prefer-enum-member
badges:
    - text: "Fixable"
      variant: success
    - text: "Suggestion"
      variant: note
description: Prefer enum member references over raw string and number values when a TypeScript enum is expected.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Replaces raw enum values with `EnumName.Member` references when the expected type points to a TypeScript enum.

## Rule details :badge[Suggestion]{variant=note}

This rule uses type information to find string and number literals that match a regular `enum` or `const enum` member.
When it can resolve a single enum match, it reports the literal and fixes it to the member reference.

It also checks object keys when the key type comes from an enum, including shapes built with `Record`, mapped types,
type aliases, and `Readonly` wrappers.

<Aside type="tip" title="Object keys are handled too">
	If an object is typed as `Record<Color, string>`, the rule can turn `Red: "#f00"` into `[Color.Red]: "#f00"`.
</Aside>

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={[
			'enum Color { Red = "Red", Blue = "Blue" }',
			"",
			"function setColor(color: Color) {}",
			'setColor("Red");',
			"",
			"const palette: Record<Color, string> = {",
			'\tRed: "#f00",',
			"};",
		].join("\n")}
		lang="ts"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={[
			'enum Color { Red = "Red", Blue = "Blue" }',
			"",
			"function setColor(color: Color) {}",
			"setColor(Color.Red);",
			"",
			"const palette: Record<Color, string> = {",
			'\t[Color.Red]: "#f00",',
			"};",
		].join("\n")}
		lang="ts"
	/>
</RuleExample>

## What gets skipped

- module specifiers like `import \"./file\"`
- directive strings like `"use strict"`
- literal types such as `type T = "Red"`
- property keys handled in places where there is no enum-typed object shape to check against

## Related rules

<CardGrid>
	<LinkCard
		title="prefer-enum-item"
		description="Uses Roblox `Enum` items instead of raw values"
		href="/eslint-cease-nonsense-rules/rules/prefer-enum-item/"
	/>
	<LinkCard
		title="prefer-pascal-case-enums"
		description="Keeps enum names consistent"
		href="/eslint-cease-nonsense-rules/rules/prefer-pascal-case-enums/"
	/>
</CardGrid>
