---
title: require-paired-calls
badges:
    - text: "error"
      variant: danger
    - text: "Fixable"
      variant: success
description: Track opener and closer calls across control flow, and report mismatches, wrong order, and unsafe yielding.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Checks opener and closer call pairs across control flow.

## Rule details :badge[Problem]{variant=danger}

The rule keeps a stack of configured opener calls and matches later closers against that stack.

It reports missing closers, extra closers, closers called out of order, early exits that skip cleanup, and branch paths
where an opener is not closed everywhere. When `requireSync` is on, it also reports `await` and `yield` between the
opener and closer.

With no options, it defaults to Roblox profiling rules:

- opener: `debug.profilebegin`
- closer: `debug.profileend`
- `requireSync: true`
- `platform: "roblox"`
- yielding functions: `task.wait`, `wait`, `*.WaitForChild`, `*.*Async`

<Aside type="danger" title="About the Fixable badge">
	The rule is marked fixable in metadata, but most reports still need a manual fix because the right cleanup code
	depends on your control flow.
</Aside>

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={[
			"function runTask() {",
			'\tdebug.profilebegin("runTask");',
			"\tif (!ready) return;",
			"\tdebug.profileend();",
			"}",
		].join("\n")}
		lang="ts"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={[
			"function runTask() {",
			'\tdebug.profilebegin("runTask");',
			"\ttry {",
			"\t\tdoWork();",
			"\t} finally {",
			"\t\tdebug.profileend();",
			"\t}",
			"}",
		].join("\n")}
		lang="ts"
	/>
</RuleExample>

## Options

| Option                    | Type                  | Default                                    |
| :------------------------ | :-------------------- | :----------------------------------------- |
| `allowConditionalClosers` | `boolean`             | `false`                                    |
| `allowMultipleOpeners`    | `boolean`             | `true`                                     |
| `maxNestingDepth`         | `number`              | `0`                                        |
| `pairs`                   | `PairConfiguration[]` | default Roblox profiling pair when omitted |

Each pair supports these fields:

| Field                | Type                 |
| :------------------- | :------------------- |
| `opener`             | `string`             |
| `closer`             | `string \| string[]` |
| `alternatives`       | `string[]`           |
| `openerAlternatives` | `string[]`           |
| `platform`           | `"roblox"`           |
| `requireSync`        | `boolean`            |
| `yieldingFunctions`  | `string[]`           |

## Related rules

<CardGrid>
	<LinkCard
		title="require-module-level-instantiation"
		description="Keeps long-lived objects out of hot paths"
		href="/eslint-cease-nonsense-rules/rules/require-module-level-instantiation/"
	/>
</CardGrid>
