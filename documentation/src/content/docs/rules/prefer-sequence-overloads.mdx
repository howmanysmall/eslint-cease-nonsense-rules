---
title: prefer-sequence-overloads
badges:
    - text: "error"
      variant: danger
    - text: "Fixable"
      variant: success
description: Prefer `ColorSequence` and `NumberSequence` constructor overloads over two-keypoint arrays.
---

import { Aside, Code, LinkCard, CardGrid } from "@astrojs/starlight/components";

import RuleExample from "../../../components/rule-example.astro";

Replaces simple two-keypoint `ColorSequence` and `NumberSequence` arrays with the shorter constructor overloads.

## Rule details :badge[Problem]{variant=danger}

This rule only reports `new ColorSequence([...])` and `new NumberSequence([...])` when all of these are true:

- the constructor gets exactly one argument, and that argument is an array
- the array has exactly two elements
- both elements are `new ColorSequenceKeypoint(...)` or `new NumberSequenceKeypoint(...)`
- the keypoint times are exactly `0` and `1`
- each keypoint has exactly two arguments, so envelope keypoints are skipped

If both endpoint values are the same, the fixer uses the single-argument overload. Otherwise it uses the two-argument
overload.

<Aside type="tip" title="What it skips">
	Sequences with middle keypoints, reversed endpoints, or envelope values are left alone.
</Aside>

## Examples

<RuleExample type="fail" title="Incorrect">
	<Code
		code={[
			"new ColorSequence([",
			"\tnew ColorSequenceKeypoint(0, Color3.fromRGB(255, 0, 0)),",
			"\tnew ColorSequenceKeypoint(1, Color3.fromRGB(0, 0, 255)),",
			"]);",
			"",
			"new NumberSequence([",
			"\tnew NumberSequenceKeypoint(0, 0),",
			"\tnew NumberSequenceKeypoint(1, 1),",
			"]);",
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

<RuleExample type="pass" title="Correct">
	<Code
		code={[
			"new ColorSequence(Color3.fromRGB(255, 0, 0), Color3.fromRGB(0, 0, 255));",
			"",
			"new NumberSequence(0, 1);",
			"new NumberSequence(42);",
			"",
			"new NumberSequence([",
			"\tnew NumberSequenceKeypoint(0, 10, 0.2),",
			"\tnew NumberSequenceKeypoint(1, 90, 0.8),",
			"]);",
		].join("\n")}
		lang="tsx"
	/>
</RuleExample>

## Related rules

<CardGrid>
	<LinkCard
		title="prefer-udim2-shorthand"
		description="Uses the shorter Roblox constructor form when the full constructor is redundant"
		href="/eslint-cease-nonsense-rules/rules/prefer-udim2-shorthand/"
	/>
	<LinkCard
		title="prefer-pattern-replacements"
		description="Applies configurable constructor and method rewrites"
		href="/eslint-cease-nonsense-rules/rules/prefer-pattern-replacements/"
	/>
</CardGrid>
